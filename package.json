{
  "name": "electron-image-viewer",
  "version": "0.1.0",
  "private": true,
  "homepage": "./",
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "electron": "^1.6.11",
    "foreman": "^2.0.0",
    "react-scripts": "1.0.10"
  },
  "main": "src/electron-starter.js",
  "scripts": {
    "start": "echo 'Avoid opening the page in the browser too' ;BROWSER=none react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "electron": "electron .",
    "dev" : "nf start -p 3000"
  }
}
